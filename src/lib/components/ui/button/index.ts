import Root from "./button.svelte";
import { type ButtonProps, type ButtonSize, type ButtonVariant, buttonVariants } from "./button.svelte";

export { Root, Root as Button, type ButtonProps, type ButtonSize, type ButtonVariant, buttonVariants };
